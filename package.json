{
  "name": "cypress_learning",
  "version": "1.0.0",
  "description": "",
  "devDependencies": {
    "cypress": "^13.2.0"
  },
  "scripts": {
    "open": "cypress open",
    "headless": "npx cypress run",
    "head": "npx cypress run --headed --browser chrome"
  }
}
